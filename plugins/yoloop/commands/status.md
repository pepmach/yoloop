---
description: Show current Yoloop harness status
argument-hint: ""
allowed-tools: Bash(yoloop:*)
---

Show the current Yoloop harness status.

Use Bash to run:

```sh
yoloop status
```

Summarize the active policy state, goal integrity, task counts, and next pending task.
