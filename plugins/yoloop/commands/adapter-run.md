Run or preview one configured Yoloop adapter role in the current project.

Use Bash to run:

```sh
yoloop adapter run $ARGUMENTS
```

Require the user to include `--role worker`, `--role critic`, or `--role grand-jury`. Use `--dry-run` when the user wants to preview the rendered command without launching the adapter.
