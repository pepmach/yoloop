Resume Yoloop policy enforcement.

Use Bash to run:

```sh
yoloop resume --actor human
```

If this fails with a goal hash mismatch, ask the user whether the edited `GOAL.html` should be accepted with `yoloop accept-goal`.
