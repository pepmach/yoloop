Run the Yoloop sequential worker-critic loop in the current project.

Use Bash to run:

```sh
yoloop run $ARGUMENTS
```

Use `--dry-run` when the user wants to preview the next task and rendered worker/critic adapter commands without launching agents or mutating task state.
